import { describe, expect, it } from "vitest";
import { partRevisionPermissions } from "./index";
const grants = (permission: { grants: readonly { role: string; dataScope: string }[] }) => new Map(permission.grants.map((grant) => [grant.role, grant.dataScope]));
describe("Slice 2B permission declarations", () => { it("keeps engineers on authoring and quality managers on review/release", () => { expect(grants(partRevisionPermissions.view)).toEqual(new Map([["ENGINEER", "ALL"], ["QUALITY_MANAGER", "ALL"], ["INSPECTOR", "ALL"], ["VIEWER", "ALL"]])); for (const permission of [partRevisionPermissions.create, partRevisionPermissions.update, partRevisionPermissions.submitReview]) expect(grants(permission)).toEqual(new Map([["ENGINEER", "ALL"]])); for (const permission of [partRevisionPermissions.return, partRevisionPermissions.approve, partRevisionPermissions.release]) expect(grants(permission)).toEqual(new Map([["QUALITY_MANAGER", "ALL"]])); }); it("defines creator review separation and an Admin-automatic-only override capability", () => { expect(partRevisionPermissions.return.separation).toBe("CREATOR_REVIEW"); expect(partRevisionPermissions.approve.separation).toBe("CREATOR_REVIEW"); expect(partRevisionPermissions.reviewOverride).toMatchObject({ code: "part_revision.review_override", grants: [], separation: "NONE" }); }); });
